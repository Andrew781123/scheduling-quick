import { makeStyles } from "@material-ui/core/styles";

const switchStyles = makeStyles({
  root: {
    marginLeft: 0,
    marginTop: 5,
    position: "absolute",
    left: "30%",
    top: 0
  },
  label: {
    color: "#868686",
    fontSize: ".8rem"
  }
});

export { switchStyles };
